#include "CmLog.h"

CmLog::CmLog(void)
{

	/*
	
		 char dateStr [9];
         char timeStr [9];
         _strdate( dateStr);
		 std::cout<<"The current date is " << dateStr;
         _strtime( timeStr );
		 std::cout<<"The current time is " << timeStr;

	 */

}

CmLog::~CmLog(void)
{
}
