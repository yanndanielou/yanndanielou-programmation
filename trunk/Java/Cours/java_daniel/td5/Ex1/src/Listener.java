import java.util.*;import javax.swing.event.*;

public class Listener implements ListSelectionListener
{
	public void valueChanged(ListSelectionEvent e)
	{
		ArrayList List = ArrayList((Colection)ListeProg.My_List.getSelectedValues());
		List.contains(arg0)
		if (List TODO)
	}
}
